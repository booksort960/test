//#define SUM 2+2
//int num = SUM * SUM;
//#define FOUR 2*2
//#define FOURS 2 * 2
#include <stdio.h>
#define	SQUARE(X) X*X
#define MAX 10
#define PSQR(X) printf("X is %d\n",((X)*(X));
#define PSQR(X) printf("#X" is %d\n",((X)*(X));

int main(void)
{

	printf("%d", MAX+MAX);
	return 0;
}